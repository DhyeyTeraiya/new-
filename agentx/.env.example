# LLM Providers (optional; falls back to mock if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20240620

# Security & Rate Limits
ALLOWED_DOMAINS=example.com,playwright.dev
COOKIE_WHITELIST=__Host-session,csrftoken
RATE_LIMIT_PER_MIN=60

# Encryption (derive key from password; keep secrets safe)
ENC_PASSWORD=change_me
